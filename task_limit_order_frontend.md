# داکیومنت تسک فرانت‌اند

## پیاده‌سازی فرم Limit Order صرافی رمز ارز

---

## مقدمه

هدف از این تسک، پیاده‌سازی یک فرم ثبت سفارش **Limit Order** برای یک صرافی رمز ارز با استفاده از **React** و **TypeScript** است. تمرکز اصلی این تسک بر روی مدیریت صحیح state، محاسبات وابسته بین ورودی‌ها، رعایت اصول **Clean Code**، بهینه‌سازی رندر کامپوننت‌ها و پیاده‌سازی UX منطقی مشابه صرافی‌های واقعی می‌باشد.

---

## مشخصات کلی پروژه

- فریم‌ورک: React
- زبان: TypeScript
- مارکت پیش‌فرض: BTC / USDT
- نیازی به پیاده‌سازی API نیست
- دارایی کاربر به‌صورت Mock پیاده‌سازی می‌شود

### تمرکز ویژه بر

- کم بودن re-render کامپوننت‌ها
- خوانایی و ساختار تمیز کد
- مدیریت state به‌صورت اصولی

---

## دارایی (Mock User Assets)

دارایی‌های کاربر به‌صورت ثابت (Mock) در نظر گرفته می‌شود:

- موجودی تتر (USDT): **100**
- موجودی بیت‌کوین (BTC): **1**

> این مقادیر فقط جهت تست و شبیه‌سازی رفتار واقعی فرم هستند.

---

## حالت‌های فرم (Buy / Sell)

فرم دارای یک **Toggle Button** برای انتخاب نوع سفارش است:

- 🟢 **Buy** (خرید بیت‌کوین با تتر)
- 🔴 **Sell** (فروش بیت‌کوین و دریافت تتر)

رفتار فرم و اسلایدر بسته به حالت انتخاب‌شده تغییر می‌کند.

---

## فیلدهای ورودی فرم

فرم شامل سه ورودی اصلی است که همگی قابل ویرایش هستند:

- **Price** (قیمت)
- **Amount** (مقدار)
- **Total** (قیمت کل سفارش)

---

## محدودیت‌های ورودی

### Amount (مقدار)

- حداقل: **0.000001**
- حداکثر: **10**

### Price (قیمت)

- حداقل: **87,510.22**
- حداکثر: **88,373.72**

### قوانین کلی تمام اینپوت‌ها

- فقط مقدار عددی مجاز است
- ورود کاراکتر غیرعددی مجاز نیست
- **مقادیر منفی مجاز نیستند**
- **تعداد ارقام اعشار محدود و از پیش تعریف‌شده است** (بر اساس نوع دارایی)
- اگر کاربر بیش از تعداد اعشار مجاز وارد کند، مقدار اینپوت نباید تغییر داده شود

#### مثال مهم (رفتار اعشار)

- برای تتر (USDT) که مثلاً فقط **۲ رقم اعشار** مجاز است:
  - اگر مقدار ورودی `0.00` باشد
  - و کاربر تلاش کند آن را به `0.000` تغییر دهد
  - **هیچ تغییری نباید در مقدار اینپوت اعمال شود**

> هدف این رفتار، جلوگیری از تجربه کاربری نامناسب و تغییر ناخواسته مقدار توسط سیستم است.

---

## منطق محاسبات بین ورودی‌ها

بین سه ورودی **Price**، **Amount** و **Total** ارتباط دوطرفه برقرار است:

### ۱. اگر Price و Amount وارد شوند

→ **Total محاسبه می‌شود**

```
Total = Price × Amount
```

### ۲. اگر Price و Total وارد شوند

→ **Amount محاسبه می‌شود**

```
Amount = Total ÷ Price
```

### ۳. اگر Amount و Total وارد شوند

→ **Price محاسبه می‌شود**

```
Price = Total ÷ Amount
```

با تغییر هر کدام از این مقادیر، سایر فیلدهای وابسته باید **بلافاصله و به‌درستی** آپدیت شوند.

---

## اسلایدر (Percentage Slider)

فرم دارای یک **Slider درصدی (۰٪ تا ۱۰۰٪)** است که میزان استفاده از دارایی کاربر را مشخص می‌کند.

---

## رفتار اسلایدر در حالت Buy

- اسلایدر بر اساس **Total** (قیمت کل سفارش) عمل می‌کند
- درصد نشان می‌دهد چه مقدار از دارایی تتر کاربر استفاده شده است

### با تغییر اسلایدر

- مقدار **Total** تغییر می‌کند
- **Amount** بر اساس **Price** محاسبه و آپدیت می‌شود

---

## رفتار اسلایدر در حالت Sell

- اسلایدر بر اساس **Amount** (مقدار بیت‌کوین) عمل می‌کند
- درصد نشان می‌دهد چه مقدار از دارایی BTC کاربر انتخاب شده است

### با تغییر اسلایدر

- مقدار **Amount** تغییر می‌کند
- **Total** بر اساس **Price** محاسبه می‌شود

---

## کارمزد (Fee)

- کارمزد به‌صورت **هاردکد ۱.۵٪** محاسبه می‌شود

### فرمول محاسبه

```
Fee = Total × 1.5%
```

---

## مقدار دریافتی کاربر (You Will Receive)

### در حالت Buy

- کاربر بیت‌کوین (**BTC**) دریافت می‌کند

```
Received BTC = Amount - Fee   (Fee بر اساس BTC)
```

### در حالت Sell

- کاربر تتر (**USDT**) دریافت می‌کند

```
Received USDT = Total - Fee
```

---

## ولیدیشن‌ها

موارد زیر **حتماً** باید بررسی و مدیریت شوند:

### ❌ عدم موجودی کافی

- در حالت **Buy**:
  - اگر **Total** بیشتر از موجودی **USDT** باشد

- در حالت **Sell**:
  - اگر **Amount** بیشتر از موجودی **BTC** باشد

در صورت نقض هر کدام از این موارد، باید پیام خطای مناسب به کاربر نمایش داده شود و امکان ثبت سفارش غیرفعال شود.

---

## خروجی مورد انتظار

- یک فرم Limit Order با رفتار مشابه صرافی‌های واقعی
- مدیریت دقیق state و وابستگی بین ورودی‌ها
- رعایت کامل محدودیت‌های عددی، اعشار و ولیدیشن‌ها
- کدی تمیز، قابل توسعه و بهینه از نظر رندر
